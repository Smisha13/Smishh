#include<studio.h>
#include<conio.h>

 int lastdigit(int n)
{
    if(n<0)
    {
        n*=-1;
        n=(n%100)/10;
        return n;
    }
    else if(n>=0&&n<=9)
    {
        
        return -1;
    }
    else
    {
        n=(n%100)/10;
        return n;
    }
}
